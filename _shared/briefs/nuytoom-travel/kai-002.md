# Brief: kai-002 — Fix API Validation
**From:** ARIA | **To:** Kai | **Priority:** High (blocking issues from QA)

## Persona
คุณคือ Kai — Senior Backend Developer ตรง จริงจัง แก้ปัญหาตรงจุด

## Files to Fix
`src/app/api/generate-plan/route.ts`

## Fix 1 (BLOCKING): เพิ่ม server-side input validation

เพิ่มก่อน process input ใดๆ:
```typescript
// Validate required fields
if (!Array.isArray(travelers) || travelers.length === 0) {
  return NextResponse.json({ error: 'ข้อมูลผู้เดินทางไม่ถูกต้อง' }, { status: 400 })
}
if (!totalBudget || totalBudget <= 0) {
  return NextResponse.json({ error: 'งบประมาณต้องมากกว่า 0' }, { status: 400 })
}
const validStyles = ['family', 'adventure', 'relaxed', 'culture']
const validAccommodation = ['budget', 'standard', 'luxury']
if (!validStyles.includes(preferences?.tripStyle) || !validAccommodation.includes(preferences?.accommodation)) {
  return NextResponse.json({ error: 'ค่าที่เลือกไม่ถูกต้อง' }, { status: 400 })
}
const d = new Date(departDate), r = new Date(returnDate)
if (isNaN(d.getTime()) || isNaN(r.getTime()) || r < d) {
  return NextResponse.json({ error: 'วันที่ไม่ถูกต้อง' }, { status: 400 })
}
```

## Fix 2 (non-blocking): Day count สอดคล้องกับ UI

Sage พบว่า route.ts คำนวณ `totalDays = diff + 1` แต่ TripForm แสดง `diff` (ไม่บวก 1)
เก็บ route.ts ไว้แบบเดิม (`+1`) เพราะถูกแล้ว — Nova จะแก้ TripForm แทน

## Fix 3 (non-blocking): Dynamic max_tokens ตามความยาวทริป

```typescript
const maxTokens = totalDays > 7 ? 16000 : 8000
// ใช้ maxTokens แทน 8000 ใน messages.create
```

## Fix 4 (non-blocking): Cap travelers ที่ 10 คน + sanitize input

```typescript
if (travelers.length > 10) {
  return NextResponse.json({ error: 'รองรับสูงสุด 10 คนต่อการจอง' }, { status: 400 })
}
// Sanitize: strip newlines จาก names
const sanitizedTravelers = travelers.map(t => ({
  name: String(t.name).replace(/[\n\r]/g, '').slice(0, 50),
  age: Math.max(0, Math.min(99, Number(t.age)))
}))
const sanitizedDestination = String(destination).replace(/[\n\r]/g, '').slice(0, 100)
// ใช้ sanitizedTravelers และ sanitizedDestination ใน prompt แทน
```

## Verification Gate
```
npx tsc --noEmit
npm run build
```
ทั้งสองต้อง pass
